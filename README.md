# 📻 Radio PS — Pepe Shneyne Broadcasting

Радио в реальном времени с WebSocket-синхронизацией для всех слушателей.

---

## 🚀 Запуск

### 1. Установка зависимостей
```bash
cd radio-ps
npm install
```

### 2. Запуск сервера
```bash
npm start
```

Или в dev-режиме с авто-перезагрузкой:
```bash
npm run dev
```

### 3. Открыть браузер
```
http://localhost:3000
```

---

## 🔑 Логины

| Роль | Логин | Пароль |
|------|-------|--------|
| Модератор | `PS` или `Pepe Shneyne` | `faa` |
| Слушатель | `user` | `1234` |
| Слушатель | `listener` | `1234` |
| Слушатель | `guest` | `0000` |

Добавить новых слушателей можно в `server.js` в объекте `LISTENERS`.

---

## 🎛 Роли

### Модератор (MOD)
- Загружает аудиофайлы (до 100 МБ)
- Добавляет текстовые объявления
- Управляет очередью воспроизведения
- Когда Мод онлайн — радио-лицо кусает губу (выбирает)
- Когда Мод офлайн — радио-лицо спит

### Слушатель
- Видит текущее состояние радио в реальном времени
- Видит анимации лица радио
- Видит тайтл играющего трека

---

## 📡 Состояния радио

| Состояние | Лицо | Когда |
|-----------|------|-------|
| `offline` | 😴 Спит + ZZZ | Мод не в сети |
| `choosing` | 😤 Кусает губу + пот | Мод онлайн, выбирает |
| `playing` | 😎 Очки + дискошар | Играет музыка |
| `text` | 😮 Открытый рот | Радио говорит текст |

---

## 🛠 Техническое

- **Express** — HTTP-сервер + статика
- **WebSocket (ws)** — реальное время для всех клиентов
- **Multer** — загрузка аудиофайлов
- Аудиофайлы хранятся в папке `uploads/` и удаляются через 24 часа
- Токены сессий хранятся в памяти (при рестарте сервера нужно перелогиниться)

---

## 🌐 Деплой (Production)

```bash
# На сервере с Node.js
PORT=3000 npm start

# Или через PM2
npm install -g pm2
pm2 start server.js --name radio-ps
pm2 save
```

Для HTTPS — поставь Nginx перед сервером или используй Cloudflare.
